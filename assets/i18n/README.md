# Locale files

Each file contains:

- `meta`: language metadata used by the app.
- `messages`: translated UI text.

Edit values only. Do not rename keys.

Run `node scripts/check-i18n.mjs` after editing.
